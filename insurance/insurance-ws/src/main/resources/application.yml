spring:
  application:
    name: insurance

  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        naming:
          physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG

security:
  jwt:
    secret: ${JWT_SECRET:VGhpcy1pcy1hLWRldi1vbmx5LWluc3VyZXRoLWJhY2tvZmZpY2Utand0LXNlY3JldC1rZXktMDE=}

airlabs:
  api:
    url: https://airlabs.co/api/v9/
    key: ${AIRLABS_API_KEY}

aviationStack:
  api:
    url: https://api.aviationstack.com/v1/
    key: ${AVIATIONSTACK_API_KEY}

flightstats:
  api:
    url: https://api.flightstats.com/flex/ratings/rest/v1/json

server:
  port: 8082
